name: flutter_edge_detection
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ffi: ^2.0.1
  flutter_rust_bridge: ^1.63.1
  file_picker: ^5.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  ffigen: ^7.2.5

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
